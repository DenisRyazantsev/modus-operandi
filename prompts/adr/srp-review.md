Review the git changes for SRP violations ONLY. Each file must have exactly one responsibility: one class per file, with no exceptions — multiple classes in one file is an SRP violation. Do NOT review ADR compliance or any other quality aspect (the review stage does that). Run git status, then git diff HEAD (includes staged changes; git diff does not show untracked files, so new files made by the executor only appear in git status — include them in the review scope). Write @STATE_DIR@/tasks/current/srp-review-@N@.md with the first line exactly 'SRP: PASS' or 'SRP: FIX' followed by actionable findings.

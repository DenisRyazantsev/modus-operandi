Review the git changes against @STATE_DIR@/tasks/current/adr.md. First read @STATE_DIR@/tasks/current/deviation.md if present: if it records a justified deviation from the ADR, evaluate the code against the ADR as amended by that deviation and do not report the deviation itself as a finding. If the deviation file is missing or unjustified, report the mismatch as a finding. Run git status, then git diff HEAD (includes staged changes; git diff does not show untracked files, so new files made by the executor only appear in git status — include them in the review scope). Write @STATE_DIR@/tasks/current/review-@N@.md with the first line exactly 'VERDICT: PASS' or 'VERDICT: FIX' followed by actionable findings. The verdict must reflect the implemented code, not the ADR document.

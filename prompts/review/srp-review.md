Find SRP violations in the code. Do not review bugs or style (a later stage does). Each file must have exactly one responsibility: one class per file, with no exceptions — multiple classes in one file is an SRP violation. Skip files with fewer than 300 lines of code. Read @STATE_DIR@/scope.txt: if it starts with 'branch-diff: ', review only that git diff; if it says 'full', review the whole codebase. Also run git status (git diff does not show untracked files, so new files appear only in git status — include them in the review scope). Write @STATE_DIR@/tasks/current/srp-review-@N@.md with the first line exactly 'SRP: PASS' or 'SRP: FIX' followed by actionable findings.

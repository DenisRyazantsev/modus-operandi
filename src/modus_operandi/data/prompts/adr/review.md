Review the git changes against @STATE_DIR@/tasks/current/adr.md. adr.md is the final version of the decision record — it already includes every agreed clarification from the run. Run git status, then git diff HEAD (includes staged changes; git diff does not show untracked files, so new files made by the executor only appear in git status — include them in the review scope). Write @STATE_DIR@/tasks/current/review-@N@.md with the first line exactly 'VERDICT: PASS' or 'VERDICT: FIX' followed by actionable findings. The verdict must reflect the implemented code, not the ADR document.

Evaluate the code against the acceptance criteria in @STATE_DIR@/tasks/current/adr.md only. plan.md is a soft document: deviations from the plan are not findings when the acceptance criteria hold.

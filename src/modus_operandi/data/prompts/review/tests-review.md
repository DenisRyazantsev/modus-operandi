Review the tests of the code in scope against your instructions. The review scope: read @STATE_DIR@/scope.txt — if it starts with 'branch-diff: ', review only that git diff; if it says 'full', review the whole codebase. Also run git status (git diff does not show untracked files, so new files appear only in git status — include them in the review scope). The machine-produced function-coverage and latency reports must already exist next to the other task artifacts (the project documents the command that produces them and where it stores them); if they are missing, run that documented command before reviewing — when scope.txt starts with 'branch-diff: <base>', the reviewed diff may be committed and thus invisible to a working-tree diff, so run the documented command against the base it names (the project's documentation states the base argument) and check the produced report actually lists the reviewed files. Write @STATE_DIR@/tasks/current/tests-review-@N@.md.

Re-review the tests against your instructions. The executor fixed the findings from @LATEST@. Verify each finding is fixed and review ONLY the changes made since the previous review: run git diff @SNAP@ and git status (git diff does not show untracked files, so new files made by the executor only appear in git status). If the code changed since the reports were produced, regenerate them with the project's documented report command before reviewing — when the review scope is a branch-diff (@STATE_DIR@/scope.txt starts with 'branch-diff: <base>'), run that command against the base it names so the reports keep covering the committed diff, not only the executor's working-tree changes. Write @STATE_DIR@/tasks/current/tests-review-@N@.md.
